const [a, b, c, d] = [3, 5, 1, 15]; // Desestruturação de array

const soma = a + b + c + d; // Soma dos elementos
const subtracao = d - b; // Subtração dos elementos
const multiplicacao = a * b; // Multiplicação dos elementos
const divisao = d / a; // Divisão dos elementos
const modulo = a % 2; // Módulo da divisão

console.log(soma, subtracao, multiplicacao, divisao, modulo); // Exibe os resultados das operações